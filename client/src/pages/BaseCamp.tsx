import styled from "styled-components";

export default function BaseCamp() {
  return (
    <BaseCampContainer>
      <div>im basecamp</div>
    </BaseCampContainer>
  );
}

const BaseCampContainer = styled.section`
  border: 2px solid black;
`;
